# -deterministic-lead-processor
Portable C11 lead-processing engine combining deterministic business rules with OpenAI-compatible LLMs. Parses CSV leads, validates structured JSON, applies pricing and routing policies, supports retries, mock/live modes, and produces normalized JSONL output with comprehensive testing and validation.
